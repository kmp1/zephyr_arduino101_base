{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "attach",
            //"preLaunchTask": "openocd_server",  Thing is, this process does not stop so if it is pre-launch, launch never happens 
            "remote": true, 
            "executable": "outdir/zephyr.elf",
            "printCalls": true,
            "cwd": "${workspaceRoot}/x86",
            "target": "remote :3333"
        }
    ]
}